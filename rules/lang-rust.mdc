---
description: Coding Standards for Rust
globs: **/*.rs, **/Cargo.toml, **/rust-toolchain.toml
alwaysApply: false
---

# Coding Standards for Rust

## Key Principles

- adhere to [Rust's naming conventions](https://rust-lang.github.io/api-guidelines/naming.html)
- embrace Rust's ownership and type system
- the standard library provides a great reference for best practices

## Tooling

- use or create `rust-toolchain.toml` files

|Check|Tool|Command|Notes|
|:-|:-|:-|:-|
|**Linting**|`clippy`|`cargo clippy --quiet --bins --tests --benches --no-deps -- -D warnings`|respect all lints defined in `Cargo.toml`; use `#[expect(...)]` instead of `#[allow(...)]`|
|**Formatting**|`rustfmt`|`cargo fmt --all --quiet --message-format=human --check`||
|**Documentation**|`rustdoc`|`RUSTDOCFLAGS="-D warnings" cargo doc --quiet --all-features --no-deps --document-private-items`|use intra-doc links; the flag turns broken links into errors|

## Paradigms

### Error Handling

- embrace `Result` and `Option` for error handling
- use `?` operator to propagate errors, especially in async functions
- use `thiserror` or `anyhow` only if already in the project; do not add them by default

### Asynchronous Programming

- prefer `async fn` over `fn -> impl Future`
- use `.await` responsibly, ensuring safe points for context switching
- follow the project's existing async runtime; do not add [`tokio`](https://docs.rs/crate/tokio/latest) (or another runtime) by default
- when the project already uses tokio:
  - use `::tokio::sync::Mutex` and `::tokio::sync::RwLock` for shared state across tasks instead of their standard-library counterparts
  - use `::tokio::task::yield_now` to yield control in cooperative multitasking scenarios
  - write unit tests with `tokio::test` for async tests
